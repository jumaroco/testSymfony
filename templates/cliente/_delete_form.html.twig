<form method="post" action="{{ path('cliente_delete', {'id': cliente.id}) }}" onsubmit="return confirm('Estas seguro de eliminar este cliente?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cliente.id) }}">
    <button class="btn btn-danger btn-sm">Eliminar</button>
</form>
